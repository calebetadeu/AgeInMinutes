<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AgeInMinutes" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yello200</item>
        <item name="colorPrimaryVariant">@color/yello300</item>
        <item name="colorAccent">@color/redAccent</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/textColor</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>